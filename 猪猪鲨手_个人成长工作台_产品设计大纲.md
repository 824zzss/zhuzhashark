# 🐷🦈 猪猪鲨手 · 个人成长工作台 — 产品设计大纲

> 定位：一款治愈系、轻量化的个人成长「外置大脑」，用左侧导航 + 右侧内容区的布局，把每日规划、健康管理、学习、副业、灵感、复盘、随手记、播客等成长事务统一收纳，督促自己每日行动、持续进步。  
> 风格参考：用户提供的「露小葵的生活」截图——奶油色/米色底、粉色点缀、圆润卡片、可爱 emoji 图标、低压迫感。

---

## 一、整体架构

### 1.1 布局结构

| 区域                   | 说明                                                                                                  |
| -------------------- | --------------------------------------------------------------------------------------------------- |
| **左侧导航栏**            | 固定宽度（约 220–260px），顶部显示品牌名「猪猪鲨手」+ 副标题，中部为 9 个功能板块入口，底部为设置/主题切换。每个入口配 emoji 图标 + 文字标签，当前选中项高亮为粉色胶囊背景。 |
| **右侧内容区**            | 顶部为日期选择器、全局搜索、快速新建按钮；下方为当前板块的具体内容与数据看板。整体采用卡片式、圆角、柔和阴影。                                             |
| **首页 Dashboard（可选）** | 未进入具体板块时，展示今日待办、健康评分、学习进度、副业收入、灵感数量、复盘状态、播客时长等关键指标的「今日概览」。                                          |

### 1.2 通用设计原则

- **低门槛**：3 步以内完成打卡、记录、新建。
- **即时反馈**：每次操作后都有视觉/数据反馈（进度条、热力图、连续天数）。
- **可视化优先**：每个板块至少提供 2 种图表/数据卡片。
- **治愈系 UI**：奶油色背景、粉色主色、圆角卡片、柔和动效、emoji 图标。
- **数据持久化**：本地优先（IndexedDB / SQLite），支持导出/备份，后续可扩展云同步。

### 1.3 数据持久化通用方案

| 层级       | 方案                             | 说明                      |
| -------- | ------------------------------ | ----------------------- |
| 本地存储     | IndexedDB（Web）/ SQLite（桌面/移动端） | 存储所有结构化数据、附件路径、用户配置。    |
| 配置存储     | localStorage / 用户配置文件          | 主题、默认视图、每日目标等轻量配置。      |
| 导出备份     | JSON / CSV / Markdown          | 支持按板块导出，便于迁移和二次加工。      |
| 附件管理     | 本地文件系统 + 缩略图索引                 | 图片、音频、合同等文件存本地，数据库只存路径。 |
| 云端同步（后续） | 可选 WebDAV / 自建同步服务             | 用户自主选择，默认不强制上云。         |

---

## 二、板块一：每日计划

> 核心目标：把一天的任务、时间块、专注力统一安排好，避免「忙盲茫」。

### 2.1 核心功能模块

1. **今日任务清单**
   - 待办 / 进行中 / 已完成三态。
   - 支持子任务、标签、优先级、截止时间。
2. **四象限优先级矩阵**
   - 按「重要 × 紧急」自动归类任务。
3. **时间块规划（Time Blocking）**
   - 以 30 分钟为粒度拖动安排任务到日历轴。
4. **番茄钟 / 专注计时**
   - 与任务绑定，记录实际专注时长。
5. **重复任务模板**
   - 每日/每周/自定义周期重复（如晨间阅读、周报）。
6. **今日概览卡片**
   - 展示剩余任务数、已完成数、专注时长。

### 2.2 数据字段

```ts
interface Task {
  id: string;
  title: string;
  description?: string;
  status: 'todo' | 'doing' | 'done';
  priority: 1 | 2 | 3 | 4;          // 1=最高
  quadrant: 'q1' | 'q2' | 'q3' | 'q4'; // 重要紧急四象限
  tags: string[];
  dueDate?: string;                // YYYY-MM-DD
  dueTime?: string;                // HH:mm
  estimatedMinutes?: number;
  actualMinutes?: number;
  repeatRule?: 'daily' | 'weekly' | 'custom' | null;
  subtasks: Subtask[];
  createdAt: string;
  completedAt?: string;
}

interface TimeBlock {
  id: string;
  date: string;
  startTime: string;
  endTime: string;
  taskId?: string;
  type: 'task' | 'break' | 'event';
  note?: string;
}

interface FocusSession {
  id: string;
  taskId?: string;
  startTime: string;
  duration: number;                // 秒
  interruptions: number;
}
```

### 2.3 交互方式

- 顶部输入框快速添加任务，回车即创建。
- 任务卡片左滑「完成」，右滑「删除」。
- 拖拽任务到四象限或时间轴中。
- 点击任务展开抽屉：编辑详情、添加子任务、启动番茄钟。
- 番茄钟提供 25/45/60 分钟预设，支持悬浮小窗。

### 2.4 可视化展示

- **今日完成率**：环形进度图。
- **四象限分布矩阵**：2×2 色块展示任务密度。
- **时间块甘特图**：日视图时间轴。
- **本周完成趋势**：折线图 / 柱状图。
- **连续计划天数**：火焰图标 + 数字。

---

## 三、板块二：健康打卡

> 核心目标：把运动、饮食、睡眠、体重、习惯等健康数据可视化，形成主动管理而非被动治疗。

### 3.1 核心功能模块

1. **运动打卡**
   - 记录运动类型、时长、强度、消耗热量、心率、感受。
2. **饮食记录**
   - 三餐 + 加餐记录，支持拍照、估算热量、营养成分。
3. **饮水打卡**
   - 自定义每日目标，点击水杯快速打卡。
4. **睡眠记录**
   - 就寝/起床时间、睡眠时长、质量评分、深睡估算。
5. **体重/体脂记录**
   - 折线图展示趋势，支持 BMI 自动计算。
6. **习惯打卡**
   - 自定义习惯（早睡、站立、眼保健操等），支持连续天数。
7. **健康评分**
   - 根据当日数据自动生成 0–100 的综合健康分。

### 3.2 数据字段

```ts
interface Exercise {
  id: string;
  date: string;
  type: 'running' | 'yoga' | 'strength' | 'swimming' | 'other';
  duration: number;                // 分钟
  intensity: 'low' | 'medium' | 'high';
  calories?: number;
  heartRateAvg?: number;
  feeling: string;
  photos?: string[];
}

interface Meal {
  id: string;
  date: string;
  mealType: 'breakfast' | 'lunch' | 'dinner' | 'snack';
  foods: { name: string; calories?: number }[];
  calories?: number;
  photos?: string[];
}

interface Sleep {
  id: string;
  date: string;
  bedTime: string;
  wakeTime: string;
  duration: number;                // 分钟
  quality: 1 | 2 | 3 | 4 | 5;
  deepSleep?: number;              // 分钟
  notes?: string;
}

interface Weight {
  id: string;
  date: string;
  weight: number;                  // kg
  bodyFat?: number;                // %
  muscle?: number;                 // kg
  bmi?: number;
}

interface Habit {
  habitId: string;
  name: string;
  icon: string;
  targetDaysPerWeek: number;
  logs: { date: string; status: boolean; note?: string }[];
}
```

### 3.3 交互方式

- 首页「一键打卡」快捷按钮，常用习惯直接点图标完成。
- 运动/饮食支持从模板快速选择（如「晨跑 5km」「早餐：鸡蛋+牛奶」）。
- 睡眠通过拖动时间条设置 bedtime/waketime。
- 体重支持手动输入或蓝牙体脂秤导入（后续扩展）。

### 3.4 可视化展示

- **体重趋势折线图**：7 天 / 30 天 / 90 天切换。
- **运动量柱状图**：按周统计不同运动类型时长。
- **睡眠质量评分图**：柱状图或表情分布。
- **习惯打卡月度热力图**：GitHub-contribution 风格。
- **健康综合评分卡片**：雷达图展示睡眠、运动、饮食、饮水、体重维度。
- **饮水进度**：水杯填充动画。

---

## 四、板块三：英语学习

> 核心目标：建立单词、听力、口语、阅读的持续学习闭环，用数据反馈提升坚持动力。

### 4.1 核心功能模块

1. **单词学习**
   - 词库管理（四六级、雅思、自定义）。
   - 基于艾宾浩斯/间隔重复算法（SRS）的每日复习任务。
   - 单词卡片：正面单词、背面释义/例句/发音。
2. **听力训练**
   - 上传/链接听力材料，记录练习时长与答题正确率。
3. **口语练习**
   - 录音跟读，记录练习次数与时长（AI 评分可后续接入）。
4. **阅读打卡**
   - 文章链接/书名、阅读时长、笔记、生词收藏。
5. **每日目标与成就**
   - 设置每日背单词数、听力时长等目标。
   - 成就徽章：初学、词汇达人、一周坚持、一月坚持等。
6. **错题本**
   - 自动收集复习中标记为「不认识」的单词。

### 4.2 数据字段

```ts
interface Word {
  id: string;
  word: string;
  phonetic?: string;
  meaning: string;
  example?: string;
  tags: string[];
  proficiency: 0 | 1 | 2 | 3 | 4 | 5; // 熟悉度
  reviewStage: number;
  nextReviewDate: string;
  wrongCount: number;
  createdAt: string;
}

interface WordBank {
  id: string;
  name: string;
  source?: string;
  tags: string[];
  wordIds: string[];
  targetDate?: string;
}

interface StudySession {
  id: string;
  date: string;
  type: 'word' | 'listening' | 'speaking' | 'reading';
  contentId?: string;
  duration: number;                // 分钟
  score?: number;
  correctCount?: number;
  totalCount?: number;
}

interface Achievement {
  id: string;
  title: string;
  description: string;
  icon: string;
  unlocked: boolean;
  unlockedDate?: string;
}
```

### 4.3 交互方式

- 单词卡片左右滑动：左滑「已掌握」，右滑「加入错题本」，上滑「发音」。
- 每日复习任务列表，点击开始闯关式学习。
- 听力/口语模块提供播放器 + 计时器。
- 阅读打卡支持粘贴链接，自动抓取标题。
- 学习完成后自动弹出「今日目标完成」庆祝动画。

### 4.4 可视化展示

- **词汇量增长曲线**：累计学习 / 掌握单词数。
- **每日学习时长柱状图**：区分单词/听力/口语/阅读。
- **掌握程度分布饼图**：生疏/熟悉/掌握比例。
- **连续学习天数**：火焰 + 日历标记。
- **成就徽章墙**：已解锁与待解锁徽章。
- **词库进度条**：每个词库的完成百分比。

---

## 五、板块四：副业赚米

> 核心目标：管理副业项目、客户、收入、成本与时间投入，让每一笔付出都清晰可追溯。

### 5.1 核心功能模块

1. **项目管理**
   - 副业/商单/外包项目卡片：名称、客户、报价、状态、进度。
2. **收入记录**
   - 每笔收入关联项目，记录金额、到账状态、支付方式、日期。
3. **支出/成本记录**
   - 工具订阅、素材购买、交通等成本。
4. **客户管理**
   - 客户档案、合作历史、付款习惯、备注。
5. **合同与发票附件**
   - 上传图片/PDF，按项目归档。
6. **时间投入记录**
   - 记录为每个项目投入的时间，计算时薪/项目利润率。
7. **财务统计**
   - 月度/年度收入、支出、净利润。

### 5.2 数据字段

```ts
interface SideProject {
  id: string;
  name: string;
  clientId?: string;
  category: string;                // 写作/设计/开发/咨询/自媒体等
  status: 'pending' | 'ongoing' | 'completed' | 'cancelled';
  quoteAmount: number;
  currency: string;
  startDate?: string;
  deadline?: string;
  actualIncome: number;
  actualCost: number;
  description?: string;
  attachments?: string[];
}

interface Income {
  id: string;
  projectId?: string;
  amount: number;
  date: string;
  paymentMethod: string;
  status: 'pending' | 'received';
  note?: string;
}

interface Expense {
  id: string;
  projectId?: string;
  category: string;
  amount: number;
  date: string;
  note?: string;
}

interface Client {
  id: string;
  name: string;
  contact?: string;
  tags: string[];
  reliabilityScore?: number;       // 1-5
  notes?: string;
}

interface TimeLog {
  id: string;
  projectId?: string;
  date: string;
  hours: number;
  description?: string;
}
```

### 5.3 交互方式

- 项目列表以卡片形式展示，顶部可按状态筛选。
- 点击项目进入详情页，可编辑信息、查看关联收入/支出/时间。
- 收入/支出通过「快速记账」浮窗录入。
- 状态流转：待执行 → 执行中 → 已完成 → 已收款。
- 支持上传合同/发票图片，长按预览。

### 5.4 可视化展示

- **收入趋势折线图/柱状图**：按月/周统计。
- **收入来源饼图**：不同副业类型占比。
- **项目利润率排行**：净利润 / 投入时间。
- **客户贡献排行**：Top 客户收入柱状图。
- **月度收支对比**：收入 vs 支出双柱图。
- **净收入卡片**：本月已到账、待收款、总利润。

---

## 六、板块五：爆款灵感

> 核心目标：像发微博一样快速捕获灵感，并通过标签、关联、状态管理把灵感转化为可执行内容。

### 6.1 核心功能模块

1. **灵感快速捕获**
   - 文字、图片、链接、语音多种输入。
2. **灵感分类**
   - 选题、标题、文案、视觉参考、热点、脚本大纲等。
3. **标签体系**
   - 多级标签（#自媒体/小红书 #生活/情绪），支持标签云。
4. **素材库**
   - 图片、截图、参考链接统一管理。
5. **灵感状态流转**
   - 原始 → 加工中 → 已使用 → 归档。
6. **从灵感到任务**
   - 一键把灵感转为一项「每日计划」或「副业项目」任务。
7. **每日回顾**
   - 随机展示历史灵感，激发新连接。

### 6.2 数据字段

```ts
interface Inspiration {
  id: string;
  title?: string;
  content: string;
  type: 'text' | 'image' | 'link' | 'audio';
  tags: string[];
  source?: string;
  mood?: string;
  status: 'raw' | 'refining' | 'used' | 'archived';
  relatedProjectId?: string;
  attachments?: string[];
  createdAt: string;
  usedAt?: string;
}

interface Tag {
  id: string;
  name: string;
  color: string;
  count: number;
}

interface Asset {
  id: string;
  filePath: string;
  type: 'image' | 'video' | 'link';
  tags: string[];
  usageCount: number;
}
```

### 6.3 交互方式

- 顶部小输入框，类似发微博：输入内容 + 添加标签 + 选择分类，回车发布。
- 图片拖拽上传或截图粘贴。
- 链接自动抓取标题和封面。
- 灵感卡片以瀑布流/网格展示，支持标签筛选。
- 右键/长按灵感：编辑、转任务、归档、删除。
- 双击建立灵感之间的双向关联。

### 6.4 可视化展示

- **标签云**：按使用频率展示标签大小/颜色。
- **灵感来源分布**：饼图（自己思考/文章/视频/对话等）。
- **月度灵感数量趋势**：折线图。
- **状态流转漏斗**：原始 → 加工中 → 已使用数量。
- **被采用灵感 TOP 排行**：按使用次数/转化任务数排序。
- **灵感热力图**：每日记录灵感数量。

---

## 七、板块六：每日复盘

> 核心目标：每天花 5–10 分钟回顾今日得失，把经验沉淀为明天的行动。

### 7.1 核心功能模块

1. **复盘模板选择**
   - KPT（Keep/Problem/Try）
   - 3R（Record/Reflect/Revise）
   - 时间轴复盘
   - 自由书写
2. **自动导入今日成就**
   - 从「每日计划」自动拉取今日已完成任务。
3. **情绪与能量记录**
   - 选择今日心情表情、能量水平 1–5 星。
4. **问题与改进**
   - 记录今天遇到的问题及下一步改进动作。
5. **明日计划生成**
   - 基于复盘自动生成明日 3 件要事。
6. **周/月复盘聚合**
   - 自动汇总一周/一月的每日复盘，生成阶段报告。

### 7.2 数据字段

```ts
interface Review {
  id: string;
  date: string;
  template: 'kpt' | '3r' | 'timeline' | 'free';
  sections: { title: string; content: string }[];
  mood: string;                    // emoji
  energy: 1 | 2 | 3 | 4 | 5;
  wins: string[];                  // 今日成就
  problems: string[];
  improvements: string[];
  tomorrowPlan: string[];
  tags: string[];
  createdAt: string;
}

interface ReviewTemplate {
  id: string;
  name: string;
  structure: { title: string; placeholder: string }[];
  isDefault: boolean;
}
```

### 7.3 交互方式

- 打开复盘页默认进入今日日期，未填写时显示模板选择。
- 选择模板后分区块填写，每块提供 placeholder 提示。
- 点击「导入今日成就」自动从任务列表拉取已完成项。
- 情绪选择用 emoji 表情面板。
- 点击「生成明日计划」把改进项转为任务并跳转到每日计划。
- 支持按日历翻看历史复盘。

### 7.4 可视化展示

- **情绪曲线折线图**：近 7/30 天心情变化。
- **复盘完成日历热力图**：每日是否完成复盘。
- **问题类型词云**：从复盘问题字段提取关键词。
- **成就数量趋势**：每周完成成就数。
- **能量水平分布**：柱状图。
- **生命之花/平衡轮雷达图**：结合各板块数据评估生活平衡度。

---

## 八、板块七：点点记录

> 核心目标：无压地随手记录碎片想法、情绪、见闻，成为个人上下文的原材料。

### 8.1 核心功能模块

1. **碎片化随手记**
   - 文字、语音、图片、位置、天气（可选）。
2. **时间轴展示**
   - 按时间倒序排列所有记录，支持日期分组。
3. **标签与搜索**
   - 快速打标签，全文搜索。
4. **每日回顾 / 随机漫步**
   - 每天推送一条历史记录，帮助 reconnect。
5. **灵感转存**
   - 把某条记录一键升级为「爆款灵感」或「复盘素材」。

### 8.2 数据字段

```ts
interface QuickNote {
  id: string;
  content: string;
  type: 'text' | 'voice' | 'image';
  tags: string[];
  location?: string;
  weather?: string;
  mood?: string;
  attachments?: string[];
  createdAt: string;
}
```

### 8.3 交互方式

- 全局悬浮「+」或快捷键快速唤起输入框。
- 语音输入自动转文字，保留原音频。
- 图片/截图直接粘贴。
- 时间轴滚动，按日期折叠/展开。
- 长按记录：打标签、转灵感、删除。
- 顶部搜索框实时过滤。

### 8.4 可视化展示

- **记录数量热力图**：GitHub 风格，显示每日记录条数。
- **标签分布图**：饼图/柱状图。
- **时间轴**：主展示形式。
- **月度记录统计**：当月记录总数、最长连续记录天数。
- **情绪/天气小图标**：时间轴中可视化展示。

---

## 九、板块八：我的播客

> 核心目标：管理播客订阅、收听进度、时间戳笔记，把听过的内容转化为知识资产。

### 9.1 核心功能模块

1. **播客订阅管理**
   - 手动添加 RSS/Feed URL，或从热门榜单搜索。
   - 支持 OPML 导入/导出。
2. **单集收听记录**
   - 记录每集收听进度、完成状态、收听时长。
3. **待播清单**
   - 把想听的单集加入播放队列。
4. **时间戳笔记 / 高光标记**
   - 在收听过程中标记精彩时刻并写笔记。
5. **收听统计**
   - 总收听时长、本周收听时长、完成率。
6. **本地播放器**
   - 播放/暂停、变速（0.5x–3x）、睡眠定时、断点续播。

### 9.2 数据字段

```ts
interface Podcast {
  id: string;
  title: string;
  author: string;
  feedUrl: string;
  cover?: string;
  category?: string;
  lastUpdated?: string;
}

interface Episode {
  id: string;
  podcastId: string;
  title: string;
  description?: string;
  duration: number;                // 秒
  publishDate: string;
  audioUrl: string;
  progress: number;                // 秒
  status: 'unplayed' | 'playing' | 'finished';
}

interface ListeningRecord {
  id: string;
  episodeId: string;
  date: string;
  listenedSeconds: number;
  completed: boolean;
}

interface EpisodeNote {
  id: string;
  episodeId: string;
  timestamp: number;               // 秒
  content: string;
  tags?: string[];
  createdAt: string;
}

interface QueueItem {
  episodeId: string;
  order: number;
}
```

### 9.3 交互方式

- 订阅列表以封面网格展示，点击展开单集列表。
- 单集卡片显示进度条、时长、发布日期。
- 点击播放进入播放器，支持后台播放。
- 收听中点击「记笔记」自动记录当前时间戳。
- 支持「高能点赞」标记精彩片段。
- 设置中可导入/导出 OPML。

### 9.4 可视化展示

- **收听时长趋势**：近 7/30 天折线图。
- **播客类型分布**：饼图。
- **单集完成率**：进度条列表。
- **周收听时长卡片**：本周 vs 上周。
- **播客订阅封面墙**：视觉化展示已订阅播客。
- **高光笔记时间轴**：单集内的标记点分布。

---

## 十、板块九：新建任务

> 核心目标：作为全局快速入口，把想法瞬间转化为具体行动，并智能分配到对应板块。

### 10.1 核心功能模块

1. **全局快速创建**
   - 浮窗/抽屉形式，随时可唤起。
2. **自然语言解析**
   - 输入「明天下午 3 点背 50 个单词」自动解析时间、内容、目标板块。
3. **目标板块选择**
   - 选择该任务归属：每日计划、健康打卡、英语学习、副业赚米等。
4. **快捷模板**
   - 常用任务模板：运动、阅读、记账、背单词、录播客等。
5. **最近标签/项目联想**
   - 自动提示最近使用的标签、客户、词库。

### 10.2 数据字段

```ts
interface TaskDraft {
  id: string;
  title: string;
  description?: string;
  targetModule: 'daily' | 'health' | 'english' | 'sideproject' | 'inspiration' | 'review' | 'note' | 'podcast';
  priority?: 1 | 2 | 3 | 4;
  dueDate?: string;
  dueTime?: string;
  tags?: string[];
  createdAt: string;
}
```

### 10.3 交互方式

- 左侧导航底部或顶部固定「+」按钮。
- 快捷键（如 Ctrl/Cmd + N）全局唤起。
- 输入框支持 @标签、#优先级、!截止时间。
- 提交后根据 targetModule 将数据写入对应表，并跳转该板块。
- 创建成功提示：「已添加到每日计划 / 英语学习」。

### 10.4 可视化展示

- **今日新增任务数**：小徽章。
- **各板块任务分布**：饼图/横向条形图。
- **快捷入口卡片**：最近使用的 4 个模板。
- **自然语言解析预览**：提交前显示解析结果确认。

---

## 十一、首页 Dashboard（今日概览）

> 当用户未选中任何板块或点击「首页」时展示，作为全局驾驶舱。

### 11.1 核心组件

| 组件          | 内容                             |
| ----------- | ------------------------------ |
| **今日问候**    | 「猪猪鲨手，早安/午安/晚安 + 今日日期 + 一句鼓励语」 |
| **今日待办**    | 今日未完成任务数 + 前 3 条任务快捷完成         |
| **健康评分**    | 今日健康综合分 + 已打卡习惯数               |
| **学习进度**    | 今日英语学习时长 / 目标、背单词数 / 目标        |
| **副业收入**    | 本月已到账 / 待收款                    |
| **灵感数量**    | 本周新增灵感数、待加工灵感数                 |
| **复盘状态**    | 今日是否已复盘，点击直达                   |
| **播客时长**    | 本周收听时长、正在收听单集                  |
| **生命之花雷达图** | 综合各板块近 7 天数据，展示生活平衡度           |

---

## 十二、技术实现建议（初版）

| 维度    | 建议                                                                           |
| ----- | ---------------------------------------------------------------------------- |
| 前端框架  | ✅ 已确认：Web 端（Vue 3 / React + Tailwind）+ PWA，响应式适配 PC 横屏与移动端竖屏。 |
| 本地数据库 | ✅ 已确认：IndexedDB + Dexie.js（本地优先），支持 JSON/CSV 导出备份。                      |
| 图表库   | ECharts / Chart.js / visx，选择支持雷达图、热力图、折线图的库。                                 |
| 状态管理  | Pinia / Zustand，按板块拆分 store。                                                 |
| 数据导出  | 每个板块支持导出 JSON/CSV，全量支持导出压缩包。                                                 |
| 附件存储  | 本地文件系统 + 缩略图生成，数据库仅保存相对路径。                                                   |
| 备份同步  | 初期可支持手动导出 + WebDAV；后续再考虑云端。                                                  |

---

## 十三、已确认决策与可行性说明（v1.1）

| # | 决策 | 可行性结论 |
|---|------|-----------|
| 1 | Web 端 + 桌面快捷方式 + 移动端桌面入口 | ✅ 可行，采用 PWA 方案，详见 13.1 |
| 2 | 数据本地存储 | ✅ IndexedDB + JSON 导出备份 |
| 3 | 内置四六级词库 | ✅ 采用开源词库（CET-4 约 4500 词 / CET-6 约 2200 词），预置进本地数据库 |
| 4 | 播客连通小宇宙 + 在线播放 | ⚠️ 可行但有限制，详见 13.2 |
| 5 | 记账自动周/月/年分析总结 | ✅ 可行，详见 13.3 |
| 6 | AI 辅助 | ✅ 可行，需配置 API Key，详见 13.4 |
| 7 | PC 横屏 + 移动端竖屏 | ✅ 响应式布局：≥1024px 显示左侧导航横屏版；<768px 折叠为底部 Tab 竖屏版 |
| 8 | 页面美化 + Emoji 图标体系 | ✅ 详见 13.5 |

### 13.1 桌面快捷方式与移动端入口（PWA 方案）

- **技术方案**：把应用做成 PWA（manifest.json + Service Worker），本地起一个轻量静态服务或直接部署。
- **PC 桌面快捷方式**：两种方式，均可行——
  1. Edge/Chrome 地址栏「安装应用」，自动生成桌面图标，独立窗口运行（推荐）；
  2. 我直接为你创建一个 Windows `.url`/`.lnk` 快捷方式指向应用地址。
- **手机端**：浏览器「添加到主屏幕」后获得独立 App 图标，全屏运行、离线可用。
- **⚠️ 需要明确的限制**：纯 Web/PWA **无法做成安卓/iOS 的真·桌面小组件（Widget）**——那需要原生开发。可实现的最接近效果是：主屏幕 App 图标 + 打开即见「今日概览」首屏（秒开、离线）。若未来确实需要 Widget，可用 Tauri/Flutter 套壳扩展。

### 13.2 小宇宙连通方案

小宇宙**没有官方开放 API**，可靠的连通路径为：

```
小宇宙节目链接（xiaoyuzhoufm.com/podcast/xxx）
   → iTunes Search API 按播客名检索（无需代理、稳定）
   → 取得 feedUrl（标准 RSS）
   → 解析 RSS 获得单集列表 + 音频直链
   → <audio> 标签在线播放（需跟随 302 重定向）
```

- **可以做到**：粘贴小宇宙链接一键订阅、拉取全部单集、在线播放、断点续播、时间戳笔记。
- **做不到 / 有风险**：同步你在小宇宙 App 内的收听历史、点赞、评论（无官方接口）；个别仅小宇宙独占且无 RSS 的节目可能无法解析（此类节目占比很低）。
- **兜底**：支持手动填 RSS 地址与 OPML 批量导入。

### 13.3 记账自动分析总结（副业赚米增强）

- **周报（每周一自动生成）**：本周收入/支出/净利润、环比上周、Top 项目、时间投入与时薪。
- **月报**：月度收支趋势、收入来源结构、客户贡献榜、目标达成率、待收款提醒。
- **年报**：年度总收入、月度曲线、最赚钱项目/客户、时薪变化、年度关键词。
- **实现**：本地定时任务（打开应用时检查补生成），报告以卡片 + 图表呈现并可导出 Markdown/图片；叠加 AI 后可生成文字版总结与建议。

### 13.4 AI 辅助能力规划

| 场景 | 功能 |
|------|------|
| 每日复盘 | 根据当天任务/健康/学习数据自动生成复盘草稿与改进建议 |
| 记账分析 | 周/月/年报的文字总结、异常支出提醒、收入优化建议 |
| 爆款灵感 | 灵感关联推荐、标题扩写、选题打分 |
| 新建任务 | 自然语言解析（"明天下午3点背50个单词" → 自动填时间/板块） |
| 英语学习 | 例句生成、单词助记故事 |
| 播客 | 单集 Shownotes 摘要提炼 |

- **实现**：设置页配置兼容 OpenAI 格式的 API（DeepSeek / 混元 / 通义等均可），Key 仅存本地；AI 为增强层，不配置 Key 时全部核心功能仍可用。

### 13.5 视觉与 Emoji 图标规范

- **配色**：奶油底 `#FFF8F0` / 卡片白 `#FFFFFF` / 主粉 `#F4C0D1`–`#D4537E` / 点缀鹅黄、薄荷绿；圆角 12–16px，柔和投影。
- **导航图标**：📅 每日计划 · 💪 健康打卡 · 📖 英语学习 · 💰 副业赚米 · 💡 爆款灵感 · 🌙 每日复盘 · ✏️ 点点记录 · 🎧 我的播客 · ➕ 新建任务
- **品牌形象**：顶部「🐷🦈 猪猪鲨手」+ 每日一句鼓励语；打卡/完成任务时触发 Emoji 撒花动效。
- **状态 Emoji**：心情（😄🙂😐😔😫）、能量（⚡×1–5）、连续打卡（🔥）、成就（🏅）。

---

## 十四、开发路线建议

| 阶段 | 内容 |
|------|------|
| Phase 1 | 整体框架 + 每日计划 + 点点记录 + 新建任务（打通本地存储与响应式布局） |
| Phase 2 | 健康打卡 + 每日复盘 + 首页 Dashboard |
| Phase 3 | 英语学习（内置四六级词库 + SRS 复习） + 副业赚米（含自动周/月/年报） |
| Phase 4 | 我的播客（小宇宙连通 + 在线播放） + 爆款灵感 |
| Phase 5 | AI 辅助层 + PWA 安装引导 + 桌面快捷方式 + 打磨动效 |

---

*文档版本：v1.1（已根据用户确认更新决策）*  
*更新日期：2026-07-27*  
*用途：供产品方向确认与后续开发依据*
